<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_quiiz"
    tools:context=".fragments.MultimediaFragment">

    <TextView
        android:id="@+id/multimediaWelcome"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Contenido Multimedia"
        android:textColor="@color/yellow_quiz"
        android:textStyle="bold"
        android:textSize="30sp"
        android:layout_margin="50dp"
        />

    <ImageView
        android:id="@+id/multimediaImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Icon"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:layout_marginLeft="30dp"
        android:layout_marginTop="150dp"
        />

    <VideoView
        android:id="@+id/multimediaVideo"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginTop="400dp"
        android:layout_marginLeft="30dp"
        />

</FrameLayout>